`test`;

const foo = `foo${bar}`;

const bar = document.querySelector(`.${className}`);

$(`#${id}`).addClass('foo');


function f1() {
  return `abc${this.message()}def`;
}

const foobar = new URI({
  path: `/${endpoint}`
});

foo[`bar`] = `foobar`;

if (`foo` === 'foo') {

}

if ((foo === bar) && (baz === qux)) {}
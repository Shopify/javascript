machine:
  node:
    version: 5.5.0

dependencies:
  pre:
    - "curl http://beyondgrep.com/ack-2.14-single-file > ~/bin/ack && chmod 0755 ~/bin/ack"
  override:
    - script/setup

test:
  override:
    - script/check
